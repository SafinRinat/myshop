{% extends "@MyShopAdmin/layout.html.twig" %}

{% block body %}
    <h3>Фотографии товара {{ product.model }}</h3>

    <a href="{{ path("myshop_admin_productphoto_addphoto", {productId:product.id}) }}">Добавить фото</a>
    <table>
    {% for photo in product.getPhotos() %}
        <tr>
            <td>{{ photo.getTitle() }}</td>
            <td>
                <img src="/photos/{{ photo.getThumbFileName() }}" />
            </td>
            <td>
                ( <a href="{{ path("myshop_admin_productphoto_editphoto", { productId:product.getId(), photoId:photo.getId() }) }}">Редактирование</a> /
                <a href="{{ path("myshop_admin_productphoto_deletephoto", { productId:product.getId(), photoId:photo.getId()}) }}">Удалить</a> )
                <a href="{{ path("myshop_admin_productphoto_sendtomail", { photoId:photo.getId()}) }}">Отослать фото на email</a> )
            </td>
        </tr>
    {% endfor %}
    </table>
{% endblock %}